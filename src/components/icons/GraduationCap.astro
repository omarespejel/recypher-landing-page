---
import LucideIcon from './LucideIcon.astro';

const { class: className = '', strokeWidth = 1.5 } = Astro.props;
---
<LucideIcon name="graduation-cap" class={className} strokeWidth={strokeWidth} />
