---
export const prerender = true;

export function getStaticPaths() {
  const locales = ['es', 'zh-CN', 'zh-TW', 'pt'];
  return locales.map((lang) => ({ params: { lang } }));
}

import Page from '../index.astro';
---

<Page />
